<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.blocking?(@user) %>    
    <%= render 'disabled_follow' %>  
  <% elsif current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
