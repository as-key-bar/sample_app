<%= form_with(model: current_user.active_blocks.build) do |f| %>
  <div><%= hidden_field_tag :blocked_id, @user.id %></div>
  <%= f.submit "Block", class: "btn btn-primary", data: { turbo_confirm: "You sure?" } %>
<% end %>