<% provide(:title, @micropost.content) %>
<div class="row">
  <div class="col-md-8">
    <%= render @micropost %>
  </div>

  <div class="col-md-8">
    <% if !@micropost.reply_to.nil? %>
      <%= render @micropost.reply_to %>
    <% end %>
  </div>

  <div class="col-md-8">
    <% if @micropost.replies.any? %>
      <ol class="microposts">
        <%= render @replies %>
      </ol>
      <%= will_paginate @replies %>
    <% end %>
  </div>


  <section class="micropost_form">
    <%= render 'shared/micropost_form', object: @reply_micropost, reply_to_id: @micropost.id %>
  </section>
</div>