<%= form_with(url: repost_path(repost), method: :delete) do |f| %>
  <%= f.button type: "submit", class: "btn btn-icon btn-icon-active btn-default", "aria-label": "リポスト解除" do %>
    <span class="glyphicon glyphicon-retweet" aria-hidden="true"></span>
  <% end %>
<% end %>
