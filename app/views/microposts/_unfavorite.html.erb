<%= form_with(model: current_user.favorites.find_by(favorited_id: micropost.id),
              html: { method: :delete }) do |f| %>
  <%= f.button type: "submit", class: "btn btn-icon btn-icon-active btn-default", "aria-label": "いいね解除" do %>
    <span class="glyphicon glyphicon-heart" aria-hidden="true"></span>
  <% end %>
<% end %>
