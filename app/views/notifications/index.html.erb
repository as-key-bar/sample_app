<% provide(:title, "Notifications") %>
<h1>Notifications</h1>

<% if @notifications.any? %>
  <ol class="microposts notifications">
    <%= render @notifications %>
  </ol>
  <%= will_paginate %>
<% else %>
  <p class="no-notifications">You have no notifications yet.</p>
<% end %>
