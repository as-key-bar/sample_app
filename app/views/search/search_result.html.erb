<% provide(:title) { @title } %>
<div class="row">
  <h1>
    検索結果: <%= @query %>
  </h1>

  <% if @microposts.any? %>
    <h3>Hit: (<%= @microposts.count %>)</h3>
    <ol class="microposts">
      <%= render @microposts %>
    </ol>
    <%= will_paginate @microposts %>
  <% end %>

</div>
